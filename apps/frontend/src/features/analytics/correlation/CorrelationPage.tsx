/**
 * Phase 3 Slice 3 D-1 — CorrelationPage T7 RED stub.
 *
 * Throws `NotImplementedError` on render so T7 component tests fail
 * at RUNTIME with a clear, traceable message rather than at module
 * import time. T9 replaces this stub with the real route container
 * orchestrating `useCorrelationSeries()` + `useCorrelation(...)` and
 * the four-leaf component tree (Filters / CaveatBanner / LagChart /
 * WarningChips).
 *
 * Per `pattern_tdd_stub_for_red_collection`. T9 GREEN flips every
 * RED test to PASS.
 */

export function CorrelationPage(): JSX.Element {
  throw new Error(
    'NotImplementedError: CorrelationPage T7 RED stub — T9 not yet implemented. '
      + 'See docs/plans/correlation-fe.md §4 T9.',
  )
}
