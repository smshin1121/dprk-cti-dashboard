"""Bootstrap ETL package — load v1.0 workbook into the Phase 0 schema.

PR #5 scope: alias dictionary, pydantic row schemas, URL canonicalization,
tag classifier, upsert repositories. Operator-facing CLI lands in PR #6.
"""
